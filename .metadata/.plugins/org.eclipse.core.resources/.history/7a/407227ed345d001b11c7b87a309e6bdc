package com.step07;

import java.util.Scanner;

public class Ex06 {

	public static void main(String[] args) {
		
		//단어의 개수
		Scanner sc = new Scanner(System.in);
		String line = sc.nextLine().toUpperCase();
		sc.close();
		
		line = line.replace(" ", "");
		int[] cnt = new int[26];
		
		for (int i=0; i<line.length(); i++) {
			cnt[line.charAt(i)-65]++;
		}
		
		int cnt = 0;
		for (int i=0; i<cnt.length; i++) {
			
			if (cnt[i] > 0) {
				
			}
		}
		
	}
}
